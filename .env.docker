# Docker Development Environment
# Diese .env wird von Docker Compose verwendet

# Database (Container)
DB_HOST=mysql-dev
DB_PORT=3306
DB_USER=app_user
DB_PASSWORD=TempPass123!
DB_NAME=pear_app_db

# Gemini API (von lokaler .env kopieren)
GEMINI_API_KEY=AIzaSyD5VCdwNwqUWb-NgaD2VHYEKn6VggoIec4
GEMINI_MODEL=gemini-1.5-pro

# Email Settings (gleich wie lokal)
IMAP_HOST=server7.rainbow-web.com
IMAP_PORT=993
IMAP_USER=postboy@pear-app.de
IMAP_PASSWORD=Birne123!
IMAP_USE_SSL=true

SMTP_HOST=server7.rainbow-web.com
SMTP_PORT=465
SMTP_USER=postboy@pear-app.de
SMTP_PASSWORD=Birne123!
SMTP_FROM="PEAR Ingest <postboy@pear-app.de>"
SMTP_USE_SSL=true

# Processing Settings
SUBJECT_KEYWORDS=Anfrage,Kundendaten,Klientendaten
REQUIRED_FIELDS=name,first_name,last_name,email,phone,address,plz,city
CASE_TAG=PEAR-

# Container-spezifisch
INGEST_URL=http://backend-dev:8000/ingest